#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @int: The integer to be computed
 * Return: The absolute value
 */
int _abs(int)
{
	if (int >= 0)
		return (int);
	else
		return (-int);
}
