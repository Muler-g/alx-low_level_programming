#include <stdio.h>
/**
 * main - a C program that prints a line using the function printf
 * Return: o
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
